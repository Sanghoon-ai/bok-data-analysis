name: Clear Cache Example

on:
  workflow_dispatch:

jobs:
  clear-cache:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Delete cache
        run: |
          rm -rf ~/.cache
          echo "Cache deleted!"
